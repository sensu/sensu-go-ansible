FROM debian:10
RUN apt-get update \
    && apt-get install -y \
         python3 python3-apt sudo bash ca-certificates iproute2 aptitude \
    && rm -rf /var/lib/apt/lists/* \
    && rm -rf /usr/share/doc \
    && rm -rf /usr/share/man \
    && apt-get clean
