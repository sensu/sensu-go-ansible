- hosts: localhost
  gather_facts: false
  collections:
    - flowerysong.sensu_go
  module_defaults:
    group/sensu_go:
      url: "http://localhost:{{ sensu_port }}/"
  tasks:
    - name: Get cluster info
      sensu_go_cluster_info:
